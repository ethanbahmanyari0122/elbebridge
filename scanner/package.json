{
  "name": "elbebridge-scan",
  "version": "0.1.0",
  "private": true,
  "description": "BFSG / GPSR / LUCID pre-check scanner over a list of domains",
  "main": "src/index.js",
  "scripts": {
    "scan": "node src/index.js",
    "intake": "node src/intake.js",
    "test": "node test/intake.js && node test/run-fixtures.js",
    "test:intake": "node test/intake.js",
    "test:scan": "node test/run-fixtures.js",
    "postinstall": "playwright install chromium"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.10.2",
    "playwright": "^1.56.0"
  }
}