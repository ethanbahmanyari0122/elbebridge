domain,group,stage,owner,updated,note
# ─────────────────────────────────────────────────────────────────────────────
# TEMPLATE. Copy this to pipeline.csv before first use:
#     cp pipeline.example.csv pipeline.csv
# pipeline.csv is deliberately NOT in version control — this repository is
# public so GitHub Pages can serve the website, and the tracker records which
# named companies we have contacted and what they said.
#
# THE TRACKER. One row per company we have touched. Ornella owns this file.
#
# It exists to stop two things: contacting the same brand twice, and generating
# a report for someone who already said no. `intake.js` reads it and refuses to
# re-add anything listed here, whatever the stage.
#
# stage:  scanned | report_sent | contacted | replied | call_booked
#         | won | lost | excluded
#
# excluded = not a prospect. Say why in the note: too small (under 10 staff and
# under €2m, so the BFSG exemption applies), already compliant, wrong market,
# or a domain that turned out to be a different company.
#
# owner:  E = Ethan, O = Ornella
# ─────────────────────────────────────────────────────────────────────────────
